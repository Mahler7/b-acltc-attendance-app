<h1>Edit Cohort</h1>

<%= form_for @cohort, url: {action: "update"} do |f| %>
  <%= render partial: "form", locals: {f: f} %>

  <%= submit_tag "Submit", class: "btn btn-default" %>
  <button type="button" class="btn btn-default"><%= link_to "Back", "/teachers" %></button>
<% end %>